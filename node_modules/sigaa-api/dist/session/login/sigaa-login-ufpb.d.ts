import { HTTP } from '../sigaa-http';
import { Page, SigaaForm } from '../sigaa-page';
import { Session } from '../sigaa-session';
import { Login } from './sigaa-login';
/**
 * Responsible for logging in UFPB.
 * @category Internal
 */
export declare class SigaaLoginUFPB implements Login {
    protected http: HTTP;
    protected session: Session;
    constructor(http: HTTP, session: Session);
    readonly errorInvalidCredentials = "SIGAA: Invalid credentials.";
    protected parseLoginForm(page: Page): SigaaForm;
    /**
     * Current login form.
     */
    protected form?: SigaaForm;
    /**
     * Retuns HTML form
     */
    getLoginForm(): Promise<SigaaForm>;
    /**
     * Start a session on desktop
     * @param username
     * @param password
     */
    protected desktopLogin(username: string, password: string): Promise<Page>;
    /**
     * Start a session on Sigaa, return login reponse page
     * @param username
     * @param password
     */
    login(username: string, password: string, retry?: boolean): Promise<Page>;
    protected parseDesktopLoginResult(page: Page): Promise<Page>;
}
