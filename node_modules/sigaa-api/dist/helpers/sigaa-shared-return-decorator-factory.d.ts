/**
 * @category Internal
 * Method decorator to cache the return,
 * it identifies the returns based on the
 * parameter id of the first argument.
 **/
export declare function sharedReturn(): (target: any, propertyKey: string, descriptor: PropertyDescriptor) => void;
