import { Parser } from '../helpers/sigaa-parser';
import { HTTP } from '../session/sigaa-http';
import { Page } from '../session/sigaa-page';
import { TeacherResult } from './sigaa-search-teacher-result';
/**
 * @category Public
 */
export interface Campus {
    name: string;
    value: string;
}
/**
 * @category Public
 */
export declare class SigaaSearchTeacher {
    private http;
    private parser;
    page: Page | null;
    constructor(http: HTTP, parser: Parser);
    loadSearchPage(): Promise<void>;
    getCampusList(): Promise<Campus[]>;
    search(teacherName: string, campus?: Campus): Promise<TeacherResult[]>;
    private parseSearchResults;
}
