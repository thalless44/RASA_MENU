import { Homework } from '../courses/resources/attachments/sigaa-homework-student';
import { StudentBond } from '../bonds/sigaa-student-bond';
import { CourseStudent } from '../courses/sigaa-course-student';
import { SigaaForm } from '../session/sigaa-page';
/**
 *
 * @category Internal
 */
export interface ActivityHomeworkData {
    courseTitle: string;
    homeworkTitle: string;
    form: SigaaForm;
    date: Date;
    done: boolean;
}
/**
 * Interface describing homework activity
 * @category Public
 */
export interface ActivityHomework {
    readonly type: 'homework';
    /**
     * Return the activity course.
     */
    getCourse(): Promise<CourseStudent>;
    /**
     * Return the activity homework.
     */
    getHomework(): Promise<Homework>;
    homeworkId: string;
    homeworkTitle: string;
    courseId: string;
    courseTitle: string;
    readonly date: Date;
    readonly done: boolean;
}
/**
 * Implements ActivityHomework
 * @category Public
 */
export declare class SigaaActivityHomework implements ActivityHomework {
    private activityData;
    private studentBond;
    readonly type = "homework";
    constructor(activityData: ActivityHomeworkData, studentBond: StudentBond);
    /**
     * @inheritdoc
     */
    get homeworkId(): string;
    /**
     * @inheritdoc
     */
    get homeworkTitle(): string;
    /**
     * @inheritdoc
     */
    get courseId(): string;
    /**
     * @inheritdoc
     */
    get courseTitle(): string;
    /**
     * @inheritdoc
     */
    getCourse(): Promise<CourseStudent>;
    /**
     * @inheritdoc
     */
    getHomework(): Promise<Homework>;
    /**
     * @inheritdoc
     */
    get date(): Date;
    /**
     * @inheritdoc
     */
    get done(): boolean;
}
