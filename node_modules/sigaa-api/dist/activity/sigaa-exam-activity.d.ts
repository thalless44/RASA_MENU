import { StudentBond } from '../bonds/sigaa-student-bond';
import { Exam } from '../courses/resources/sigaa-exam-student';
import { CourseStudent } from '../courses/sigaa-course-student';
/**
 *
 * @category Internal
 */
export interface ActivityExamData {
    courseTitle: string;
    examDescription: string;
    date: Date;
    done: boolean;
}
/**
 *
 * @category Public
 */
export interface ActivityExam {
    readonly type: 'exam';
    /**
     * Return the activity course.
     */
    getCourse(): Promise<CourseStudent>;
    /**
     * Return the activity exam.
     */
    getExam(): Promise<Exam>;
    examDescription: string;
    courseTitle: string;
    readonly date: Date;
    readonly done: boolean;
}
export declare class SigaaActivityExam implements ActivityExam {
    private activityData;
    private studentBond;
    readonly type = "exam";
    constructor(activityData: ActivityExamData, studentBond: StudentBond);
    /**
     * @inheritdoc
     */
    get examDescription(): string;
    /**
     * @inheritdoc
     */
    get courseTitle(): string;
    /**
     * @inheritdoc
     */
    getCourse(): Promise<CourseStudent>;
    /**
     * @inheritdoc
     */
    getExam(): Promise<Exam>;
    /**
     * @inheritdoc
     */
    get date(): Date;
    /**
     * @inheritdoc
     */
    get done(): boolean;
}
