import { Quiz } from '../courses/resources/attachments/sigaa-quiz-student';
import { StudentBond } from '../bonds/sigaa-student-bond';
import { CourseStudent } from '../courses/sigaa-course-student';
import { SigaaForm } from '../session/sigaa-page';
/**
 *
 * @category Internal
 */
export interface ActivityQuizData {
    courseTitle: string;
    quizTitle: string;
    id: string;
    form: SigaaForm;
    date: Date;
    done: boolean;
}
/**
 *
 * @category Public
 */
export interface ActivityQuiz {
    readonly type: 'quiz';
    /**
     * Return the activity course.
     */
    getCourse(): Promise<CourseStudent>;
    getQuiz(): Promise<Quiz>;
    quizId: string;
    quizTitle: string;
    courseId: string;
    courseTitle: string;
    readonly date: Date;
    readonly done: boolean;
}
export declare class SigaaActivityQuiz implements ActivityQuiz {
    private activityData;
    private studentBond;
    readonly type = "quiz";
    constructor(activityData: ActivityQuizData, studentBond: StudentBond);
    /**
     * @inheritdoc
     */
    get quizId(): string;
    /**
     * @inheritdoc
     */
    get quizTitle(): string;
    /**
     * @inheritdoc
     */
    get courseId(): string;
    /**
     * @inheritdoc
     */
    get courseTitle(): string;
    /**
     * @inheritdoc
     */
    getCourse(): Promise<CourseStudent>;
    /**
     * @inheritdoc
     */
    getQuiz(): Promise<Quiz>;
    /**
     * @inheritdoc
     */
    get date(): Date;
    /**
     * @inheritdoc
     */
    get done(): boolean;
}
