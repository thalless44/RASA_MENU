import { CourseForum } from './/forum/sigaa-course-forum-student';
import { Homework } from './attachments/sigaa-homework-student';
import { LinkAttachment } from './attachments/sigaa-link-student';
import { Quiz } from './attachments/sigaa-quiz-student';
import { Survey } from './attachments/sigaa-survey-student';
import { VideoAttachment } from './attachments/sigaa-video-student';
import { WebContent } from './attachments/sigaa-web-content-student';
import { File } from '../../resources/sigaa-file';
import { UpdatableResourceData } from '../../resources/sigaa-resource-manager';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../resources/updatable-resource';
/**
 * @category Public
 */
export declare type Attachment = File | Homework | Quiz | CourseForum | WebContent | Survey | LinkAttachment | VideoAttachment;
/**
 * @category Internal
 */
export interface LessonData extends UpdatableResourceData {
    title: string;
    id: string;
    contentText: string;
    startDate: Date;
    endDate: Date;
    attachments: Attachment[];
}
/**
 * @category Public
 */
export interface Lesson extends UpdatableResource<LessonData> {
    readonly title: string;
    readonly id: string;
    readonly contentText: string;
    readonly endDate: Date;
    readonly startDate: Date;
    readonly attachments: Attachment[];
}
/**
 * @category Internal
 */
export declare class SigaaLesson extends AbstractUpdatableResource implements Lesson {
    private _title;
    private _id;
    private _contextText;
    private _startDate;
    private _endDate;
    private _attachments;
    constructor(options: LessonData, updater?: UpdatableResourceCallback);
    update(options: LessonData): void;
    get title(): string;
    get id(): string;
    get contentText(): string;
    get endDate(): Date;
    get startDate(): Date;
    get attachments(): Attachment[];
}
