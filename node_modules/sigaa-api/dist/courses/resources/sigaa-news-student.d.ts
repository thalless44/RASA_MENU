import { Parser } from '../../helpers/sigaa-parser';
import { UpdatableResourceData } from '../../resources/sigaa-resource-manager';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../resources/updatable-resource';
import { HTTP } from '../../session/sigaa-http';
import { SigaaForm } from '../../session/sigaa-page';
/**
 * @category Internal
 */
export interface NewsData extends UpdatableResourceData {
    title: string;
    form: SigaaForm;
    id: string;
}
/**
 * @category Public
 */
export interface News extends UpdatableResource<NewsData> {
    readonly title: string;
    getContent(): Promise<string>;
    getDate(): Promise<Date>;
}
/**
 * @category Internal
 */
export declare class SigaaNews extends AbstractUpdatableResource implements News {
    private http;
    private parser;
    private _form;
    private _title;
    private _content?;
    private _date?;
    constructor(http: HTTP, parser: Parser, options: NewsData, updater: UpdatableResourceCallback);
    update(newsOptions: NewsData): void;
    get id(): string;
    get title(): string;
    getContent(): Promise<string>;
    getDate(): Promise<Date>;
    private getFullNews;
}
