import { UpdatableResourceData } from '../../../resources/sigaa-resource-manager';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../../resources/updatable-resource';
import { HTTP } from '../../../session/sigaa-http';
import { SigaaForm } from '../../../session/sigaa-page';
/**
 * @category Internal
 */
export interface QuizData extends UpdatableResourceData {
    title: string;
    id: string;
    startDate: Date;
    endDate: Date;
    formSendAnswers?: SigaaForm;
    formViewAnswersSubmitted?: SigaaForm;
}
/**
 * @category Public
 */
export interface Quiz extends UpdatableResource<QuizData> {
    readonly title: string;
    readonly type: 'quiz';
    readonly endDate: Date;
    readonly startDate: Date;
    readonly id: string;
    /**
     * TODO
     * @param retry
     */
    getAnswersSubmitted(): Promise<void>;
}
/**
 * @category Internal
 */
export declare class SigaaQuiz extends AbstractUpdatableResource implements Quiz {
    private http;
    readonly type = "quiz";
    readonly errorDeadlineToReadClosed = "SIGAA: Deadline to read as answer closed.";
    readonly errorQuizYetNoSendAnswers = "Sigaa: Quiz yet no sent answers.";
    private _formViewAnswersSubmitted?;
    private _formSendAnswers?;
    private _startDate;
    private _endDate;
    private _title;
    private _id;
    constructor(http: HTTP, options: QuizData, updater: UpdatableResourceCallback);
    update(options: QuizData): void;
    get title(): string;
    get id(): string;
    get endDate(): Date;
    get startDate(): Date;
    /**
     * TODO
     * @param retry
     */
    getAnswersSubmitted(retry?: boolean): Promise<void>;
}
