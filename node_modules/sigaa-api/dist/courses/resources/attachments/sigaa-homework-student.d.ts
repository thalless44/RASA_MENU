import { HTTP } from '../../../session/sigaa-http';
import { SigaaForm } from '../../../session/sigaa-page';
import { File } from '../../../resources/sigaa-file';
import { CourseResourcesFactory } from '../../sigaa-course-resources-factory';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../../resources/updatable-resource';
import { UpdatableResourceData } from '../../../resources/sigaa-resource-manager';
/**
 * @category Internal
 */
export interface HomeworkData extends UpdatableResourceData {
    title: string;
    startDate: Date;
    endDate: Date;
    id?: string;
    description?: string;
    haveGrade?: boolean;
    isGroupHomework?: boolean;
    formSendHomework?: SigaaForm;
    formViewHomeworkSubmitted?: SigaaForm;
}
/**
 * @category Internal
 */
export interface Homework extends UpdatableResource<HomeworkData> {
    readonly type: 'homework';
    readonly title: string;
    readonly id: string | null;
    getFlagHaveGrade(): Promise<boolean>;
    getFlagIsGroupHomework(): Promise<boolean>;
    readonly startDate: Date;
    readonly endDate: Date;
    /**
     * Get attachment file.
     */
    getAttachmentFile(): Promise<File>;
}
/**
 * @category Internal
 */
export declare class SigaaHomework extends AbstractUpdatableResource implements Homework {
    private http;
    private courseResourcesFactory;
    readonly type = "homework";
    private _title;
    private _startDate;
    private _endDate;
    private _id?;
    private _formSendHomework?;
    private _formViewHomeworkSubmitted?;
    private _description?;
    private _haveGrade?;
    private _isGroupHomework?;
    private _file?;
    constructor(http: HTTP, courseResourcesFactory: CourseResourcesFactory, options: HomeworkData, updater: UpdatableResourceCallback);
    update(options: HomeworkData): void;
    get title(): string;
    get id(): string | null;
    getFlagHaveGrade(): Promise<boolean>;
    getFlagIsGroupHomework(): Promise<boolean>;
    getDescription(): Promise<string>;
    get endDate(): Date;
    get startDate(): Date;
    /**
     * @inheritdoc
     */
    getAttachmentFile(): Promise<File>;
}
