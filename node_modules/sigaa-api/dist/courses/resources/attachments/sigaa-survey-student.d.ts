import { UpdatableResourceData } from '../../../resources/sigaa-resource-manager';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../../resources/updatable-resource';
import { SigaaForm } from '../../../session/sigaa-page';
/**
 * @category Internal
 */
export interface SurveyData extends UpdatableResourceData {
    id: string;
    title: string;
    form: SigaaForm;
}
/**
 * @category Public
 */
export interface Survey extends UpdatableResource<SurveyData> {
    readonly type: 'survey';
    readonly id: string;
    readonly title: string;
}
/**
 * @category Internal
 */
export declare class SigaaSurvey extends AbstractUpdatableResource implements Survey {
    readonly type = "survey";
    private _title;
    private _form;
    constructor(options: SurveyData, updater: UpdatableResourceCallback);
    update(options: SurveyData): void;
    get title(): string;
    get id(): string;
}
