import { Parser } from '../../../helpers/sigaa-parser';
import { UpdatableResourceData } from '../../../resources/sigaa-resource-manager';
import { HTTP } from '../../../session/sigaa-http';
import { SigaaForm } from '../../../session/sigaa-page';
import { AbstractUpdatableResource, UpdatableResource, UpdatableResourceCallback } from '../../../resources/updatable-resource';
/**
 * @category Internal
 */
export interface WebContentData extends UpdatableResourceData {
    title: string;
    id: string;
    form: SigaaForm;
    date?: Date;
}
/**
 * @category Internal
 */
export interface WebContent extends UpdatableResource<WebContentData> {
    readonly type: 'webcontent';
    getDate(): Promise<Date>;
    getContent(): Promise<string>;
    readonly title: string;
}
/**
 * @category Internal
 */
export declare class SigaaWebContent extends AbstractUpdatableResource implements WebContent {
    private http;
    private parser;
    readonly type = "webcontent";
    private _title;
    private form;
    private _content;
    private _date;
    constructor(http: HTTP, parser: Parser, options: WebContentData, updater: UpdatableResourceCallback);
    update(options: WebContentData): void;
    getDate(): Promise<Date>;
    get title(): string;
    getContent(): Promise<string>;
    private loadWebContentPage;
    get id(): string;
}
