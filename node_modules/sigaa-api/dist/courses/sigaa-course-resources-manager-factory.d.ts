import { CourseResourcesFactory } from './/sigaa-course-resources-factory';
import { CourseResourcesManager } from './/sigaa-course-resources-manager';
import { HTTP } from '../session/sigaa-http';
import { CourseStudent } from './sigaa-course-student';
/**
 * Abstraction to represent the class that create instances of CourseResourceManager.
 * @category Internal
 */
export interface CourseResourcesManagerFactory {
    createCourseResourcesManager(http: HTTP, course: CourseStudent): CourseResourcesManager;
}
/**
 * Class to create instances of CourseResourceManager.
 * @category Internal
 */
export declare class SigaaCourseResourceManagerFactory implements CourseResourcesManagerFactory {
    private courseResourcesFactory;
    constructor(courseResourcesFactory: CourseResourcesFactory);
    createCourseResourcesManager(http: HTTP, course: CourseStudent): CourseResourcesManager;
}
